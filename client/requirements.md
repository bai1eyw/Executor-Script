## Packages
date-fns | Formatting dates for execution logs
framer-motion | Smooth transitions and animations for dashboard elements
lucide-react | Icons for the UI (already in base but noting usage)
react-hook-form | Form handling for script editing
zod | Schema validation
clsx | Class name utility
tailwind-merge | Class merging utility
prismjs | Syntax highlighting for code viewing (optional but good for this) or just simple pre/code blocks styled well

## Notes
- Tailwind config should extend fonts to include 'JetBrains Mono' or 'Fira Code' for the terminal look.
- The app requires polling for execution status updates.
